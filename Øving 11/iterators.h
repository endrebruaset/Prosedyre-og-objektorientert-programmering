#include <vector>
#include <iostream>
#include <string>
#include <algorithm>
#include <set>
using namespace std;

void vectorForwardIterator(vector<string>&);

void vectorReverseIterator(vector<string>&);

void vectorReplace(vector<string>&, string old, string replacement);

void setForwardIterator(set<string>&);

void setReverseIterator(set<string>&);

void setReplace(set<string>&, string old, string replacement);